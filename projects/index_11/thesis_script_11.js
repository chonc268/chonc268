document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card1")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card2")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card3")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card4")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card5")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card6")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card7")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card8")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card9")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card10")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card11")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card12")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card13")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card14")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card15")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card16")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card17")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card18")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card19")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card20")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card21")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card22")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card23")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card24")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card25")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card26")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card27")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card28")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card29")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card30")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card31")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card32")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card33")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card34")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card35")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card36")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card37")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card38")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card39")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / 25
  const angleY = (targetX - x) / -25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})

document.addEventListener("mousemove", function (event) {
  const x = event.pageX
  const y = event.pageY
  const target = document.querySelector(".card40")
  const targetCoords = target.getBoundingClientRect()
  const targetX = targetCoords.left + (target.offsetWidth / 2)
  const targetY = targetCoords.top + (target.offsetHeight / 2)
  const angleX = (targetY - y) / -25
  const angleY = (targetX - x) / 25
  target.style.transform = "rotateX(" + angleX + "deg) rotateY(" + angleY + "deg)"
})
